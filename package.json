{
    "name": "ts-kube",
    "version": "0.1.2",
    "description": "Typed kubernetes resource definitions",
    "main": "target",
    "types": "target/index.d.ts",
    "files": [
        "target"
    ],
    "scripts": {
        "prepublishOnly": "npm run build",
        "clean": "rm -rf 'target'",
        "build": "npm run clean; tsc",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "repository": "https://github.com/jsen-/ts-kube",
    "keywords": [],
    "author": "Martin Ivicic",
    "license": "MIT OR Apache-2.0",
    "devDependencies": {
        "typescript": "^3.6.4"
    }
}
